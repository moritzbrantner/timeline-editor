# Domain Context

This repository uses a single domain context.

Agents should read the root `CONTEXT.md` first. It is the canonical entrypoint for repository boundaries and task-specific documentation.

There is no `docs/adr/` directory yet. If ADRs are added later, read relevant ADRs before changing architecture, workflow, issue-tracker behavior, labels, or automation.

Consult the docs listed in `CONTEXT.md` for architecture, core behavior, extensions, serialization, compute contracts, and workbench behavior.
